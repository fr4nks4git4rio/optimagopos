# Hardening — hunt6 — HTML/JS sinks in Livewire Blade views

coverage_id: `resources/views/livewire/**/*.blade.php#HTML/JS sinks::resources/views/components/select2-ajax.blade.php#inline script URL injection::profile/quick/all-in-scope-subsystems::ATTACK-CLASSES.md#Injection`

| # | coverage_id (sub-scope) | boundary | sink | source | gap | wallet_grants_tier | proposed_fix | proposed_fix_tests |
|---|--------------------------|----------|------|--------|-----|--------------------|--------------|--------------------|
| 1 | `resources/views/livewire/**/*.blade.php#HTML/JS sinks` | select2-ajax inline script URL injection (research/dev) | `resources/views/components/select2-ajax.blade.php::L43::L67::L93::L113::L137` `url: '{!! $url !!}'` | All 38 callers pass `route()`-built constants; sibling components use `{{$url}}` — no attacker input reaches the URL today | Unescaped raw interpolation in inline JS string; a future caller that builds a URL from user data would get a JS breakout | low (0_russian, day_0_paper, year_leaks_30m, small_grants_higher_bounty) | Swap `{!! !!}` for `{{ }}` like the modals variant | Unit assertion compiling `route('clientes.load-clientes', [...])` through a select2-ajax render and asserting no `'` appearance in output; render check on escaped variant tests |
| 2 | `resources/views/livewire/**/*.blade.php#HTML/JS sinks` | trazas activity-log columns (priority — related to candidate S-8) | `resources/views/livewire/trazas/index.blade.php::L48-L50` `{!! $log->{log_name,description,causer_name} !!}` | activity_log description logs embed `Crypt::decrypt(nombre_comercial)` from no-RFC client/comensal saves; causer_name embeds user nombre/apellidos/email | Raw output of attacker-controlled (decrypted) strings into HTML | medium-high (upgrades with S-8) | Output-escape all three columns (`{{ }}`) or sanitize at write time (strip HTML from description); treat decrypt() outputs as untrusted | Browser smoke test of `/admin/trazas` with a planted no-RFC client; PHPunit `php artisan test` plus a curl of the blade with a payload asserting no `<` survives |
| 3 | `resources/views/livewire/cuentas-cobrar/index.blade.php` | x-html sink (research/dev) | `resources/views/livewire/cuentas-cobrar/index.blade.php::L268..L382` 15 `x-html` cells | `FacturaController::loadCuentasCobrar` — admin-authored system invoices only today | If system-invoice creation ever accepts external client data (e.g., wiring parseData from POS ingest into del_sistema=1), the x-html cells become injectable | low | Escape rendered columns (`{{ }}`) / return structured data and escape in the view | Regression test seeding a del_sistema=1 factura whose cliente razon_social contains `<` and asserting the rendered blade does not output the raw character |
| 4 | `resources/views/livewire/layouts/toast.blade.php` | toast message (research/dev) | `resources/views/livewire/layouts/toast.blade.php::L18` `{!! $message !!}` | All `show-toast` dispatches today are static translations or same-session literal messages | Raw interpolation of a browser-controlled message prop; only self-session impact observed | low | Use `{{ $message }}` | Render a toast with message `<img src=x onerror=alert(1)>` and assert escaped output |
| 5 | `resources/views/livewire/facturas*/{timbrar,cancel}.blade.php` + `facturas/save.blade.php::L53` + `reports/excel/ingresos.blade.php::L37-L42` + `reports/factura/pdf.blade.php::L195` | residual raw sinks (research/dev) | 4 files | Dead `text_alert` props; DB-date validation warning; encrypted ciphertext columns; admin-neglected `comentarios` | Raw `{!! !!}` inventory carries user data only under admin authorship; no injection today | low | Remove/escape dead props; switch remaining `{!! !!}` to `{{ }}` except intentional static `__()` text | grep-based lint enforcing no `{!! !!}` on model-derived variables; `view:cache` smoke test per page |